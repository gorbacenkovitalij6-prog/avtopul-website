# Настройка Telegram бота для автоматической обработки заявок

## 📋 Содержание
1. [Создание бота](#создание-бота)
2. [Получение токена и Chat ID](#получение-токена-и-chat-id)
3. [Настройка переменных окружения](#настройка-переменных-окружения)
4. [Автоматические уведомления](#автоматические-уведомления)
5. [Продвинутая настройка](#продвинутая-настройка)
6. [Автоответы и команды](#автоответы-и-команды)

---

## 1. Создание бота

### Шаг 1: Откройте BotFather в Telegram
1. Найдите [@BotFather](https://t.me/BotFather) в поиске Telegram
2. Нажмите **Start** или отправьте `/start`

### Шаг 2: Создайте нового бота
Отправьте команду:
```
/newbot
```

### Шаг 3: Настройте бота
BotFather попросит вас указать:

1. **Имя бота** (отображаемое имя):
   ```
   Автопул - Пригон авто
   ```

2. **Username бота** (должен заканчиваться на `bot`):
   ```
   avtopul_saratov_bot
   ```

### Шаг 4: Получите токен
После создания BotFather отправит вам сообщение с **токеном**:
```
Done! Congratulations on your new bot. You will find it at t.me/avtopul_saratov_bot
Use this token to access the HTTP API:
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz1234567890
```

⚠️ **ВАЖНО:** Сохраните этот токен в надежном месте!

---

## 2. Получение токена и Chat ID

### Вариант А: Личные сообщения (рекомендуется для начала)

1. **Найдите вашего бота** в Telegram по username
2. **Нажмите Start** или отправьте `/start`
3. **Получите Chat ID**:
   - Откройте в браузере:
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
   ```
   - Замените `<YOUR_BOT_TOKEN>` на ваш токен
   - Найдите `"chat":{"id":123456789}` - это ваш Chat ID

**Пример ответа:**
```json
{
  "ok": true,
  "result": [
    {
      "update_id": 123456789,
      "message": {
        "message_id": 1,
        "from": {
          "id": 987654321,
          "is_bot": false,
          "first_name": "Иван"
        },
        "chat": {
          "id": 987654321,  ← ВАШ CHAT ID
          "first_name": "Иван",
          "type": "private"
        },
        "date": 1234567890,
        "text": "/start"
      }
    }
  ]
}
```

### Вариант Б: Группа или канал (для команды)

1. **Создайте группу** в Telegram
2. **Добавьте бота** в группу как участника
3. **Сделайте бота администратором** группы:
   - Откройте настройки группы
   - Администраторы → Добавить администратора
   - Выберите вашего бота
   - Дайте права "Удаление сообщений" (минимум)

4. **Получите Chat ID группы**:
   - Напишите любое сообщение в группу
   - Откройте в браузере:
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
   ```
   - Найдите `"chat":{"id":-100123456789}` - это Chat ID группы
   - **Важно:** Chat ID группы начинается с минуса!

**Пример для группы:**
```json
{
  "chat": {
    "id": -1001234567890,  ← CHAT ID ГРУППЫ (с минусом!)
    "title": "Автопул - Заявки",
    "type": "supergroup"
  }
}
```

---

## 3. Настройка переменных окружения

### Локальная разработка

Откройте файл `.env.local` в корне проекта и добавьте:

```env
# Telegram Bot Configuration
NEXT_PUBLIC_TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz1234567890
NEXT_PUBLIC_TELEGRAM_CHAT_ID=987654321
```

**Для группы:**
```env
NEXT_PUBLIC_TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz1234567890
NEXT_PUBLIC_TELEGRAM_CHAT_ID=-1001234567890
```

### Деплой на Netlify

1. Откройте **Netlify Dashboard**
2. Выберите ваш проект
3. Перейдите в **Site settings → Environment variables**
4. Добавьте две переменные:
   - `NEXT_PUBLIC_TELEGRAM_BOT_TOKEN` = ваш токен
   - `NEXT_PUBLIC_TELEGRAM_CHAT_ID` = ваш chat ID
5. Нажмите **Save**
6. **Redeploy** сайт

---

## 4. Автоматические уведомления

### Текущая реализация

Сейчас при отправке формы на сайте бот отправляет уведомление в Telegram:

```
🚗 Новая заявка с сайта БТА ТОРГ

👤 Имя: Иван Иванов
📱 Телефон: +7 (900) 123-45-67
📧 Email: ivan@example.com

💬 Сообщение:
Интересует BMW X5, 2020 год, бюджет до 40000 евро
```

### Проверка работы

1. Заполните форму на сайте
2. Нажмите "Отправить заявку"
3. Проверьте Telegram (личные сообщения или группу)
4. Вы должны получить уведомление!

### Отладка проблем

Если уведомления не приходят:

1. **Проверьте токен и Chat ID:**
   ```bash
   # Отправьте тестовое сообщение
   curl -X POST "https://api.telegram.org/bot<YOUR_TOKEN>/sendMessage" \
        -d "chat_id=<YOUR_CHAT_ID>" \
        -d "text=Test message"
   ```

2. **Проверьте консоль браузера:**
   - Откройте DevTools (F12)
   - Вкладка Console
   - Отправьте форму
   - Проверьте ошибки

3. **Проверьте права бота:**
   - Для группы: бот должен быть администратором
   - Для канала: бот должен иметь право публиковать

---

## 5. Продвинутая настройка

### Улучшенное форматирование сообщений

Обновите файл `src/app/api/send-telegram/route.ts`:

```typescript
const text = `
🚗 *Новая заявка с сайта Автопул*

━━━━━━━━━━━━━━━━━━━━
👤 *Клиент:* ${name}
📱 *Телефон:* \`${phone}\`
📧 *Email:* ${email || "Не указан"}

💬 *Сообщение:*
${message}

━━━━━━━━━━━━━━━━━━━━
🕐 *Время:* ${new Date().toLocaleString('ru-RU', { timeZone: 'Europe/Saratov' })}
`.trim();

const response = await fetch(telegramUrl, {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    chat_id: chatId,
    text: text,
    parse_mode: "Markdown", // Включаем Markdown форматирование
  }),
});
```

### Добавление кнопок-действий

```typescript
const keyboard = {
  inline_keyboard: [
    [
      { text: "✅ Связаться", url: `https://t.me/${phone.replace(/[^0-9]/g, '')}` },
      { text: "📧 Email", url: `mailto:${email}` }
    ],
    [
      { text: "📋 Обработано", callback_data: "processed" },
      { text: "❌ Отклонить", callback_data: "reject" }
    ]
  ]
};

const response = await fetch(telegramUrl, {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    chat_id: chatId,
    text: text,
    parse_mode: "Markdown",
    reply_markup: keyboard, // Добавляем кнопки
  }),
});
```

### Уведомления о расчетах калькулятора

Добавьте endpoint для отправки расчетов:

```typescript
// src/app/api/send-calculation/route.ts
export async function POST(request: Request) {
  const { carPrice, country, engineVolume, totalCost } = await request.json();

  const text = `
💰 *Новый расчет стоимости*

━━━━━━━━━━━━━━━━━━━━
🚗 *Стоимость авто:* €${carPrice}
🌍 *Страна:* ${country}
⚙️ *Объем двигателя:* ${engineVolume}л

💵 *Итого:* ~€${totalCost}

━━━━━━━━━━━━━━━━━━━━
👉 Клиент запросил расчет, свяжитесь для детальной консультации
`.trim();

  // Отправка в Telegram
}
```

---

## 6. Автоответы и команды

### Создание бота с автоответами (Python)

Для автоматических ответов клиентам создайте отдельного бота:

```python
# bot.py
from telegram import Update
from telegram.ext import Application, CommandHandler, MessageHandler, filters

BOT_TOKEN = "your_bot_token"

async def start(update: Update, context):
    """Приветствие при команде /start"""
    await update.message.reply_text(
        "🚗 Добро пожаловать в Автопул!\n\n"
        "Мы специализируемся на пригоне автомобилей из Европы.\n\n"
        "Напишите ваш вопрос, и мы ответим в ближайшее время!"
    )

async def help_command(update: Update, context):
    """Справка"""
    await update.message.reply_text(
        "📋 *Доступные команды:*\n\n"
        "/start - Начать диалог\n"
        "/price - Рассчитать стоимость\n"
        "/contact - Наши контакты\n"
        "/help - Эта справка",
        parse_mode="Markdown"
    )

async def contact(update: Update, context):
    """Контактная информация"""
    await update.message.reply_text(
        "📞 *Контакты Автопул:*\n\n"
        "☎️ Телефон: +7 (845) 200-00-00\n"
        "📧 Email: info@avtopul.ru\n"
        "📍 Адрес: г. Саратов, ул. Международная, 26\n\n"
        "🕐 Работаем: Пн-Пт 09:00-20:00, Сб-Вс 10:00-18:00",
        parse_mode="Markdown"
    )

def main():
    app = Application.builder().token(BOT_TOKEN).build()

    app.add_handler(CommandHandler("start", start))
    app.add_handler(CommandHandler("help", help_command))
    app.add_handler(CommandHandler("contact", contact))

    print("Бот запущен!")
    app.run_polling()

if __name__ == "__main__":
    main()
```

### Установка и запуск (Python):

```bash
# Установите библиотеку
pip install python-telegram-bot

# Запустите бота
python bot.py
```

### Создание бота на Node.js

```javascript
// bot.js
const TelegramBot = require('node-telegram-bot-api');

const token = 'your_bot_token';
const bot = new TelegramBot(token, {polling: true});

// Команда /start
bot.onText(/\/start/, (msg) => {
  const chatId = msg.chat.id;
  bot.sendMessage(chatId,
    '🚗 Добро пожаловать в Автопул!\n\n' +
    'Мы специализируемся на пригоне автомобилей из Европы.\n\n' +
    'Напишите ваш вопрос, и мы ответим в ближайшее время!'
  );
});

// Команда /contact
bot.onText(/\/contact/, (msg) => {
  const chatId = msg.chat.id;
  bot.sendMessage(chatId,
    '📞 *Контакты Автопул:*\n\n' +
    '☎️ Телефон: +7 (845) 200-00-00\n' +
    '📧 Email: info@avtopul.ru\n' +
    '📍 Адрес: г. Саратов, ул. Международная, 26',
    {parse_mode: 'Markdown'}
  );
});

// Обработка всех сообщений
bot.on('message', (msg) => {
  const chatId = msg.chat.id;

  // Автоответ на сообщения
  if (!msg.text.startsWith('/')) {
    bot.sendMessage(chatId,
      'Спасибо за ваше сообщение! Наш менеджер свяжется с вами в ближайшее время. ' +
      'Обычно мы отвечаем в течение 15 минут в рабочее время.'
    );
  }
});

console.log('Бот запущен!');
```

### Установка и запуск (Node.js):

```bash
# Установите библиотеку
npm install node-telegram-bot-api

# Запустите бота
node bot.js
```

---

## 📊 Мониторинг и аналитика

### Отслеживание заявок

Добавьте в группу/канал закрепленное сообщение со статистикой:

```
📊 Статистика за сегодня:
✅ Обработано: 12
⏳ В работе: 3
📞 Связались: 8
```

### Интеграция с Google Sheets

Для автоматического сохранения заявок в таблицу:

```typescript
// src/app/api/send-telegram/route.ts
// Добавьте отправку в Google Sheets
const sheetsData = {
  date: new Date().toISOString(),
  name,
  phone,
  email,
  message
};

// Используйте Google Sheets API для записи
```

---

## ✅ Чеклист настройки

- [ ] Создан бот через @BotFather
- [ ] Получен токен бота
- [ ] Получен Chat ID (личный или группы)
- [ ] Добавлены переменные в `.env.local`
- [ ] Проверена отправка тестового сообщения
- [ ] Протестирована форма на сайте
- [ ] Настроены переменные на Netlify
- [ ] Бот добавлен в группу (если используется)
- [ ] Бот сделан администратором группы
- [ ] Настроены автоответы (опционально)

---

## 🆘 Помощь и поддержка

### Частые ошибки

**1. "Chat not found"**
- Проверьте, что бот получил хотя бы одно сообщение
- Для группы: убедитесь, что бот добавлен и является админом

**2. "Unauthorized"**
- Неверный токен
- Проверьте, что токен скопирован полностью

**3. "Bad Request: message is too long"**
- Сообщение превышает 4096 символов
- Разбейте на несколько сообщений

### Полезные ссылки

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [BotFather команды](https://core.telegram.org/bots#6-botfather)
- [Примеры ботов](https://github.com/python-telegram-bot/python-telegram-bot/tree/master/examples)

---

**Создано для ООО "Автопул" | 2025**
